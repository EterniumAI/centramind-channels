{
  "name": "centramind-channels",
  "version": "0.1.0",
  "description": "Channel adapter for Centramind agents -- Telegram webhook ingress + severity-routed outbox",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.87.0"
  }
}
